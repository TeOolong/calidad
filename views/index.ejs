<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>App Covid</title>
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>
  <main>
      <div class="main__container">
          <div class="information__container">
              <div class="information__container-login">
                  <h3>¿Ya tienes una cuenta?</h3>
                  <p>Inicia sesión para entrar</p>
                  <button id="switch_login">Iniciar Sesión</button>
              </div>
              <div class="information__container-register">
                  <h3>¿Aún no tienes una cuenta?</h3>
                  <p>Regístrate para que puedas iniciar sesión</p>
                  <button id="switch_register">Regístrarse</button>
              </div>
          </div>


          <div class="forms__container">
            <form name="login_form" action="" method="" class="forms__container-login">
              <h2>Iniciar sesión</h2>
              <input type="number" name="dni" maxlength="8"
              oninput="javascript: if (this.value.length > this.maxLength) this.value = this.value.slice(0, this.maxLength);" placeholder="DNI">
              <input type="password" name="password" placeholder="Contraseña">
              <button id="login">Ingresar</button>
              <div id="alert_login" class="alert alert-danger" role="alert"></div>
            </form>

            <form name="register_form" action="" method="" class="forms__container-register">
              <h2>Registrarse</h2>
              <input type="number" name="dni" maxlength="8"
              oninput="javascript: if (this.value.length > this.maxLength) this.value = this.value.slice(0, this.maxLength);" placeholder="DNI">
              <input type="password" name="password" placeholder="Contraseña">
              <input type="password" name="repassword" placeholder="Repetir Contraseña">
              <input type="number" name="verification" maxlength="1"
              oninput="javascript: if (this.value.length > this.maxLength) this.value = this.value.slice(0, this.maxLength);" placeholder="Codigo Verificación">
              <button id="register">Registrarse</button>
              <div id="alert_register" class="alert alert-danger" role="alert"></div>
            </form>
          </div>
      </div>

  </main>

  <script src="/js/index.js"></script>
</body>
</html>